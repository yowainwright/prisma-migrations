---
import { CopyButton } from "../common/CopyButton";
import { resolveDocsUrl } from "../../utils/urlResolver";
---

<div
  class="hero text-base-content mx-auto py-12 sm:py-16 lg:py-20 px-4 sm:px-6 max-w-md md:max-w-full"
>
  <div class="hero-content text-center">
    <div class="w-full max-w-xl">
      <h1 class="text-3xl sm:text-4xl md:text-5xl font-bold">
        Install <span class="gradient-text">Prisma Migrations</span>
      </h1>

      <p class="mt-4 sm:mt-6 w-full max-w-lg text-center text-base">
        Install prisma-migrations globally, as a devDependency,<br class="hidden sm:inline" />
        <span class="sm:hidden">or use with npx.</span>
        <span class="hidden sm:inline">or just use with npx.</span>
      </p>
      <div
        class="flex items-center bg-transparent border border-subtle rounded-lg shadow-sm mt-4 justify-between p-2 min-w-0 overflow-hidden"
      >
        <code class="ml-2 sm:ml-3 text-sm sm:text-base truncate sm:whitespace-nowrap">npm install -g prisma-migrations</code>
        <CopyButton client:only="react" />
      </div>

      <a href={resolveDocsUrl("setup")} class="inline-block mt-6 sm:mt-7">
        <button class="btn text-base px-3 py-2 h-12 whitespace-nowrap text-white border-0 shadow-none outline-none hover:opacity-90 gradient-bg">Setup guide</button>
      </a>
    </div>
  </div>
</div>
